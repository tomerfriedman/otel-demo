import { Resource } from '@opentelemetry/resources';
import { ResourceSyncDetectionConfig } from './config';
/**
 * Runs all SYNC resource detectors and returns the results merged into a single
 * Resource.
 *
 * @param config Configuration for resource detection
 */
export declare const detectSyncResources: (config?: ResourceSyncDetectionConfig) => Resource;
//# sourceMappingURL=detect-sync-resources.d.ts.map